import dotenv from "dotenv";import {Pool} from "pg";dotenv.config();if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL is required");const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});await pool.query(`create table if not exists users(id bigserial primary key,username text unique,created_at timestamptz default now());create table if not exists orders(id bigserial primary key,public_id text unique not null,name text,telegram text,status text not null default 'new',total_from numeric default 0,created_at timestamptz default now());`);await pool.end();console.log("Migration complete");
